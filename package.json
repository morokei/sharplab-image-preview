{
  "name": "sharplab-image-preview",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "ts-node-esm index.ts",
    "build": "tsc && shx cp iisnode/*.* dist && shx cp package*.json dist && cd dist && npm ci --production --omit=dev && 7z a -tzip app.zip ."
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "express-async-handler": "^1.2.0",
    "generic-pool": "^3.8.2",
    "puppeteer": "^15.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "7zip": "^0.0.6",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
